import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Social & Messenger Node | Autonomous DM Engagement | ConversionIQ',
  description: 'Transform Instagram and Facebook engagement into revenue. Turn every comment, mention, and DM into a goal-oriented sales dialogue.',
};

export default function SocialMessengerNodeLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

